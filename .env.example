# Exemplo de arquivo de variáveis de ambiente para o projeto
# Copie este arquivo para `.env.local` ou `.env` e ajuste os valores conforme necessário.

# URL base da API usada pelo frontend (sem barra final) — exemplo de desenvolvimento:
VITE_API_URL=http://localhost:8080/api

# Habilita o uso de mocks no frontend (true|false). Quando true, a camada de API usa dados simulados.
VITE_USE_MOCKS=true

# Observação: variáveis usadas pelo Vite devem começar com `VITE_` para serem expostas ao código do cliente.